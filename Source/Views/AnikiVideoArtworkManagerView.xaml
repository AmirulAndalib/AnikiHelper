<UserControl x:Class="AnikiHelper.AnikiVideoArtworkManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vc="clr-namespace:AnikiHelper.Services.VideoPlayer"
             UseLayoutRounding="True"
             SnapsToDevicePixels="True"
             TextOptions.TextFormattingMode="Display"
             RenderOptions.BitmapScalingMode="Fant">
    <UserControl.Resources>
        <Style x:Key="ArtworkManagerTextBox" TargetType="TextBox">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="CaretBrush" Value="{DynamicResource TextBrush}"/>
            <Setter Property="Background" Value="{DynamicResource ButtonBackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource NormalBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="SelectionBrush" Value="{DynamicResource GlyphBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="InputBorder" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="InputBorder" Property="BorderBrush" Value="{DynamicResource GlyphBrush}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="InputBorder" Property="BorderBrush" Value="{DynamicResource GlyphBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ArtworkManagerButton" TargetType="Button">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="Background" Value="{DynamicResource ButtonBackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource NormalBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="14,5"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}" RecognizesAccessKey="True"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource HoverBrush}"/>
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="{DynamicResource GlyphBrush}"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="{DynamicResource GlyphBrush}"/>
                                <Setter TargetName="ButtonBorder" Property="BorderThickness" Value="2"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.72"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ArtworkManagerChoice" TargetType="ListBoxItem">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Margin" Value="0,0,14,16"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20" Background="{DynamicResource WindowBackgourndBrush}" TextElement.Foreground="{DynamicResource TextBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,14">
            <TextBlock Text="{DynamicResource VideoLibraryManager_SearchArtwork}" FontSize="24" FontWeight="SemiBold"/>
            <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                <TextBlock x:Name="ArtworkTargetText" FontWeight="SemiBold"/>
                <TextBlock Text="  •  " Opacity="0.45"/>
                <TextBlock x:Name="TargetTitleText" Opacity="0.7"/>
            </StackPanel>
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,0,0,14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="ArtworkSearchBox" Style="{StaticResource ArtworkManagerTextBox}" MinHeight="34" VerticalContentAlignment="Center" KeyDown="ArtworkSearchBox_KeyDown"/>
            <Button Grid.Column="1" x:Name="SearchButton" Style="{StaticResource ArtworkManagerButton}" Content="{DynamicResource VideoLibraryManager_Search}" MinWidth="110" MinHeight="34" Margin="10,0,0,0" Click="SearchButton_Click"/>
        </Grid>

        <Grid Grid.Row="2">
            <ListBox x:Name="ArtworkChoicesList"
                     Foreground="{DynamicResource TextBrush}"
                     Background="Transparent"
                     ItemsSource="{Binding Choices}"
                     SelectedItem="{Binding SelectedChoice, Mode=TwoWay}"
                     BorderThickness="0"
                     ItemContainerStyle="{StaticResource ArtworkManagerChoice}"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     ScrollViewer.VerticalScrollBarVisibility="Auto">
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel IsItemsHost="True"/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                <ListBox.ItemTemplate>
                    <DataTemplate DataType="{x:Type vc:AnikiVideoLibraryArtworkChoice}">
                        <Border x:Name="ChoiceCard" Width="150" Background="{DynamicResource ButtonBackgroundBrush}" BorderThickness="1" BorderBrush="{DynamicResource NormalBorderBrush}" CornerRadius="5" Padding="5">
                            <StackPanel>
                                <Border x:Name="ChoiceImage" Height="215" Background="#19000000" CornerRadius="3" ClipToBounds="True">
                                    <Image x:Name="ChoicePreviewImage"
                                           Source="{Binding PreviewPath}"
                                           Stretch="UniformToFill"
                                           RenderOptions.BitmapScalingMode="Fant"
                                           SnapsToDevicePixels="True"
                                           UseLayoutRounding="True"/>
                                </Border>
                                <TextBlock Text="{Binding MatchText}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" Margin="1,7,1,0" ToolTip="{Binding MatchText}"/>
                                <TextBlock Margin="1,2,1,0" FontSize="11" Opacity="0.7">
                                    <Run Text="{Binding ProviderText}"/><Run Text="  •  "/><Run Text="{Binding LanguageText}"/>
                                </TextBlock>
                                <TextBlock Text="{Binding SizeText}" FontSize="11" Opacity="0.6" Margin="1,2,1,0"/>
                            </StackPanel>
                        </Border>
                        <DataTemplate.Triggers>
                            <DataTrigger Binding="{Binding ArtworkTarget}" Value="landscape">
                                <Setter TargetName="ChoiceCard" Property="Width" Value="230"/>
                                <Setter TargetName="ChoiceImage" Property="Height" Value="130"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding ArtworkTarget}" Value="hero">
                                <Setter TargetName="ChoiceCard" Property="Width" Value="230"/>
                                <Setter TargetName="ChoiceImage" Property="Height" Value="130"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding ArtworkTarget}" Value="logo">
                                <Setter TargetName="ChoiceCard" Property="Width" Value="230"/>
                                <Setter TargetName="ChoiceImage" Property="Height" Value="130"/>
                                <Setter TargetName="ChoicePreviewImage" Property="Stretch" Value="Uniform"/>
                            </DataTrigger>
                        </DataTemplate.Triggers>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            <TextBlock x:Name="ArtworkStatusText" HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="0.72" FontSize="16" TextWrapping="Wrap" TextAlignment="Center"/>
        </Grid>

        <Grid Grid.Row="3" Margin="0,14,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="{DynamicResource VideoLibraryManager_ArtworkApplyHelp}" VerticalAlignment="Center" Opacity="0.65" TextWrapping="Wrap"/>
            <Button Grid.Column="1" x:Name="LocalFileButton" Style="{StaticResource ArtworkManagerButton}" MinWidth="132" MinHeight="34" Margin="12,0,0,0" Click="LocalFileButton_Click"/>
            <Button Grid.Column="2" x:Name="ApplyButton" Style="{StaticResource ArtworkManagerButton}" Content="{DynamicResource VideoLibraryManager_Apply}" MinWidth="110" MinHeight="34" Margin="8,0,0,0" Click="ApplyButton_Click" IsEnabled="False"/>
            <Button Grid.Column="3" Style="{StaticResource ArtworkManagerButton}" Content="{DynamicResource VideoLibraryManager_Cancel}" MinWidth="100" MinHeight="34" Margin="8,0,0,0" Click="CancelButton_Click"/>
        </Grid>
    </Grid>
</UserControl>
